{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  xmlns:xhtml="http://www.w3.org/1999/xhtml">
{{- range .Data.Pages -}}
  {{- /* Тонкие term-страницы (теги/категории с <2 постами) Google всё равно
         не индексирует — выкидываем из sitemap, чтобы не съедать crawl budget
         и не раздувать «обнаружена, не проиндексирована» в GSC. */ -}}
  {{- if and (eq .Kind "term") (lt (len .Pages) 2) -}}{{- continue -}}{{- end }}
  {{- /* Всё, что помечено noindex во фронтматтере, из sitemap тоже убираем:
         отдавать роботу карту с запрещёнными к индексации URL — это прямая
         дорога к «Отправленный URL помечен тегом noindex» в GSC. */ -}}
  {{- if .Params.robotsNoIndex -}}{{- continue -}}{{- end }}
  <url>
    <loc>{{ .Permalink }}</loc>
    {{- if not .Lastmod.IsZero }}
    <lastmod>{{ safeHTML (.Lastmod.Format "2006-01-02T15:04:05-07:00") }}</lastmod>
    {{- end }}
    {{- with .Sitemap.ChangeFreq }}
    <changefreq>{{ . }}</changefreq>
    {{- end }}
    {{- if ge .Sitemap.Priority 0.0 }}
    <priority>{{ .Sitemap.Priority }}</priority>
    {{- end }}
  </url>
{{- end }}
</urlset>
